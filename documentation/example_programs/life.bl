(set-context "grid" 10) ;; 10x10 grid
;; grid-context should provide array of cells
(grid-cells)

;; colours
(let alive "white")

;; loop through each cell and check neighbors
(func (update-state) (true)
	(if ()))

;; handle click 
(func (toggle-cell cell)
 ("implement me"))

;; in grid context - clicked cell is passed to event handler
(on:click (toggle-cell cell))

;; animate
(animate update-state 1000) ;; animate every second