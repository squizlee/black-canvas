;; defining and manipulating objects in blisp

(let x (obj 
	name "ethan"
	test true
))

(let y (obj
	pet (obj name "polly" subpet (obj name "jeff"))
	loved true))

(debug x)
(debug x.name)


(debug y)
(debug y.pet.name)